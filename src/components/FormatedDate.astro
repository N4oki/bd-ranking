---
export interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="text-xs  sm:text-xl">
  {
    date.toLocaleDateString("ja", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
